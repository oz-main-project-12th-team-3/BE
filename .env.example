# Database settings for Django
DB_NAME=mydatabase
DB_USER=myuser
DB_PASSWORD=mypassword
DB_HOST=db
DB_PORT=5432

# Django secret key (IMPORTANT: Change this for production!)
SECRET_KEY=django-insecure-default-key

# Debug mode (set to 0 for production)
DEBUG=1

# Allowed hosts for Django (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1

# Gmail account setting
EMAIL_HOST_USER=your_email@example.com
EMAIL_HOST_PASSWORD=your_email_password_or_app_password
